<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" /> <!-- цвет фона -->
    <stroke
        android:width="1dp"
    android:color="@color/logo_color" /> <!-- цвет границы при фокусе -->
    <corners android:radius="8dp" /> <!-- закругленные углы -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" /> <!-- отступы внутри поля -->
</shape>